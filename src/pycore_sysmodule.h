/* wasthon stub — sys module internals not in the bridge. Helpers pulled
 * in by stdlib modules (e.g. _pickle's _PySys_GetSizeOf) are declared in
 * wasthon.h on demand. */
#ifndef WASTHON_PYCORE_SYSMODULE_H
#define WASTHON_PYCORE_SYSMODULE_H
#include "wasthon.h"
#endif
