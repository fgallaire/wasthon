/* wasthon stub — set/frozenset internals not in the bridge. The few
 * helpers stdlib modules pull in (e.g. _pickle's _PySet_NextEntry) are
 * declared in wasthon.h on demand. */
#ifndef WASTHON_PYCORE_SETOBJECT_H
#define WASTHON_PYCORE_SETOBJECT_H
#include "wasthon.h"
#endif
