/* Wasthon shim for CPython's pycore_runtime.h.
 * Only _Py_ID() (already provided via wasthon.h) and per-module state
 * helpers used by _pickle. Empty for now — _Py_ID is in wasthon.h. */
#ifndef Py_INTERNAL_RUNTIME_H
#define Py_INTERNAL_RUNTIME_H
#endif
